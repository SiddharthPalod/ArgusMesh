use uuid::Uuid;

pub const SERVICE_UUID: Uuid = Uuid::from_bytes([
    0x12, 0x34, 0x56, 0x78, // Arbitrary for now
    0x90, 0xab, 0xcd, 0xef,
    0x12, 0x34, 0x56, 0x78,
    0x90, 0xab, 0xcd, 0xef,
]);

pub const CHARACTERISTIC_INBOX_UUID: Uuid = Uuid::from_bytes([
    0xab, 0xcd, 0xef, 0x12, 
    0x34, 0x56, 0x78, 0x90,
    0xab, 0xcd, 0xef, 0x12,
    0x34, 0x56, 0x78, 0x90,
]); // Write without response for incoming messages

// Notify characteristic for pushing events?
// Or we just use INBOX as bidirectional (Write + Notify)?
// Let's assume INBOX supports WriteWithoutResponse and Notify.
